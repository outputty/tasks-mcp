{
  "name": "tasks-mcp",
  "version": "0.1.0",
  "description": "outputty's task tracker as a local MCP server, backed by GitHub Issues with two-way sync.",
  "type": "module",
  "private": false,
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --hot run src/server.ts",
    "test": "bun test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^4.6.14",
    "octokit": "^4.0.2"
  },
  "devDependencies": {
    "prettier": "^3.9.4"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
